## Server customizations
machine:
  node:
    version: 4.4.7

## Customize dependencies
dependencies:
  pre:
    - npm install -g bower
    - npm install -g phantomjs@2.1.1
  override:
    - npm install
    - bower install

## Deployment settings
deployment:
  staging:
    branch: master
    heroku:
      appname: atlanta-ember-client
